# OpenAI API Key - Required for AI parsing and analysis (Phase 1+)
OPENAI_API_KEY=sk-proj-YOUR-KEY-HERE

# Note: Get your key from https://platform.openai.com/api-keys

# JWT Secret Key - Required for authentication
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here

# Encryption Key - Required for securing stored credentials
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# Encryption Salt - Required when using passphrase-based encryption
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Note: If ENCRYPTION_KEY is a proper Fernet key (44 chars ending in =), this is not needed
ENCRYPTION_SALT=your-unique-deployment-salt-here

# CORS Origins - Required for production deployment
# Comma-separated list of allowed origins (no spaces)
# Example: CORS_ORIGINS=https://analize.online,https://www.analize.online
CORS_ORIGINS=https://your-domain.com

# Google OAuth (Optional - for "Sign in with Google" feature)
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable "Google+ API" and "Google Identity Toolkit API"
# 4. Go to "Credentials" > "Create Credentials" > "OAuth 2.0 Client IDs"
# 5. Choose "Web application"
# 6. Add authorized JavaScript origins: http://localhost:5173 (for dev)
# 7. Copy the Client ID below
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# CAPTCHA Solving Service API Keys (Optional - only needed for automated CAPTCHA solving)
# Get keys from: https://2captcha.com/ or https://anti-captcha.com/
# These are only used when automated CAPTCHA solving is needed
TWOCAPTCHA_API_KEY=
ANTICAPTCHA_API_KEY=
CAPTCHA_SERVICE=2captcha  # '2captcha' or 'anticaptcha'
