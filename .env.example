# OpenAI API Key - Required for AI parsing and analysis (Phase 1+)
OPENAI_API_KEY=sk-proj-YOUR-KEY-HERE

# Note: Get your key from https://platform.openai.com/api-keys

# Encryption Key - Required for securing stored credentials
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# Google OAuth (Optional - for "Sign in with Google" feature)
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable "Google+ API" and "Google Identity Toolkit API"
# 4. Go to "Credentials" > "Create Credentials" > "OAuth 2.0 Client IDs"
# 5. Choose "Web application"
# 6. Add authorized JavaScript origins: http://localhost:5173 (for dev)
# 7. Copy the Client ID below
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
