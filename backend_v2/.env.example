# Database
DATABASE_URL=postgresql://user:password@localhost/healthy

# Authentication
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# OpenAI (for AI parsing and health analysis)
OPENAI_API_KEY=sk-your-openai-api-key

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Encryption Key (for credentials and sensitive data)
ENCRYPTION_KEY=your-fernet-key-generate-with-cryptography

# Email Service (SMTP)
# For AWS SES:
#   SMTP_HOST=email-smtp.eu-central-1.amazonaws.com
#   SMTP_PORT=587
#   SMTP_USER=your-ses-smtp-username
#   SMTP_PASS=your-ses-smtp-password
#
# For Gmail (development):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-specific-password
#
# For other providers:
#   SendGrid: smtp.sendgrid.net:587
#   Mailgun: smtp.mailgun.org:587
#   Brevo: smtp-relay.brevo.com:587

SMTP_HOST=email-smtp.eu-central-1.amazonaws.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@analize.online

# Application URL (for email links)
APP_URL=https://analize.online
